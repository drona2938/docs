---
projectName: "{{projectName}}"
version: "{{version}}"
---

# Welcome to 

This is version  of the project.

import { useConfig } from "@mintlify/docs";

const { projectName, version } = useConfig();

# Welcome to {projectName}

This is version {version} of the project.